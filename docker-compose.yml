services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: joker-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - joker-data:/data

volumes:
  joker-data:
